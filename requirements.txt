# Versões fixadas para reprodutibilidade (instalação nova em produção
# não deve trazer uma versão diferente da testada). Ao atualizar,
# testar em homologação antes de subir — em especial pandas e
# mysql-connector-python, que já tiveram bumps de major recentes.
pandas==2.3.3
openpyxl==3.1.5
mysql-connector-python==9.7.0
jinja2==3.1.6
python-dotenv==1.2.3
sqlalchemy==2.0.52
premailer==3.10.0
matplotlib==3.10.8
zabbix-utils==2.0.4
